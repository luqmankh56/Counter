let count = 0;
let saveEL = document.getElemementById("save-el")
let countEL = document.getElemementById("count-el")

  function increment() {
    count += 1
    countEl.textContent= count
  }

  function save() {
    let countStr = count + " - "
    saveEl.innerTextStr
    countEL.textCotent= 0
    count = 0
  }
  console.log("Lets count people on the subway!")
  
